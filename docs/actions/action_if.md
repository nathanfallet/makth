# If action

## Kotlin syntax

```kotlin
IfAction(
    condition,
    listOf(
        // To execute if condition is evaluated to true
    ),
    listOf(
        // To execute else
    )
)
```

## Makth lexer syntax

```
if (condition) {

} else {
    
}
```
